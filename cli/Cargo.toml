[package]
# Seeded from plugin.json; subsequent bumps are owned by tasks/version.py,
# which keeps plugin.json / this Cargo.toml / checksums.json coherent.
name = "luminosity"
version = "0.1.0-pre.0"
edition = "2021"

[lints]
workspace = true

[[bin]]
name = "luminosity"
path = "src/main.rs"
